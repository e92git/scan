package scanService

import (
	"scan/app/service/scan/model"
)

func (c *Config) All() ([]model.Scan, error) {
	// l := locationService.New(c.db)
	// fmt.Println(l)
	// locations, err := c.repository.All()
	// if err != nil {
	// 	return nil, err
	// }

	// return locations, nil
	return nil, nil
}
